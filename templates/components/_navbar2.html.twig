{# COMPOSANT NAVBAR
Ce fichier contient le code necessaire a l'affichage de la navbar
 #}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="#"><img src="{{asset('img/BecomeChefLogo.png')}}" alt="logo"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul
				class="navbar-nav me-auto mb-2 mb-lg-0">
				{# on verifie que l'utilisateur est bien connecte pour afficher l'onglet Profil dans la navbar #}
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link " href="{{path('profile')}}">Profil</a>
					</li>
				{% endif %}

				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{path('home')}}">Home</a>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Recettes
					</a>
					<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
						<li>
							{# on verifie que l'utilsiateur est bien admin pour afficher le catalogue admin #}
							{% if app.user and "ROLE_ADMIN" in app.user.roles %}
								<a class="dropdown-item" href="{{path('recette_admin',{'page':1 })}}">Catalogue Admin</a>
								{# si il ne l'est pas, on affiche le catalogue  #}
							{% else %}
								<a class="dropdown-item" href="{{path('recette',{'page':1 })}}">Catalogue</a>
							{% endif %}
						</li>
                        <li>
						{# on verifie que l;utilisateur est bien connecte pour afficher ces onglets #}
						{% if app.user %}
							{# s'il est connecte et qu'il est admin , on affiche les onglets admin #}
							{% if "ROLE_ADMIN" in app.user.roles %}
								<a class="dropdown-item" href="{{path('new_recette_admin')}}">Ajouter Admin</a>
							
							{# sinon l'onglet user #}
						{% else %}
							<a class="dropdown-item" href="{{path('new_recette')}}">Ajouter</a>
						{% endif %}
					</li>
				</li>
				<li>
					<a class="dropdown-item" href="{{path('list')}}">Favoris</a>
				</li>
			{% endif %}
		</li>
	</ul>
</li>
<li class="nav-item">
	<a class="nav-link " href="{{path('contact')}}">Contact</a>
</li>
{# on verifie que l'utilisateur est connecte pour afficher le bouton de decc #}
{% if app.user %}
	<li>
		<a href="{{path('app_logout')}}" class="btn btn-outline-danger mx-2">deconexion</a>
	</li>
	{# sinon on affiche l'inscription ou la connexion #}
{% else %}
	<li>
		<a href="{{path('app_register')}}" class="btn btn-outline-success ">Register</a>
	</li>
	<li>
		<a href="{{path('app_login')}}" class="btn btn-outline-success mx-2">Login</a>
	</li>

{% endif %}</ul>

<form class="d-flex" action="{{path('search')}}">
<input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" name="search_value">
<button class="btn btn-outline-success" type="submit">Search</button></form></div></div>
</nav>
