{# COMPOSANT NAVBAR
	Ce fichier contient le code necessaire a l'affichage de la navbar
	 #}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
	<a class=" mb-0 text-center navbar-brand" href="{{path('home')}}"><img src="{{asset('img/BecomeChefLogo.png')}}" width="70px" height="70px" class="rounded-circle align-self-center" alt="logo"></a>
		
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{path('home')}}">Home</a>
				</li>
				{% if app.user and "ROLE_ADMIN" in app.user.roles  %}
					<li class="nav-item">
						<a class="nav-link " href="{{path('espaceAdmin',{'tableau': 'recettes', 'page': 1})}}">Espace Admin</a>
					</li>
				{% endif %}

				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link " href="{{path('profile',{'page':1 })}}">Profil</a>
					</li>
				{% endif %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="recetteMenu" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Recettes
					</a>
					<ul class="dropdown-menu" aria-labelledby="recetteMenu">
						{% if app.user and "ROLE_ADMIN" in app.user.roles %}
							<li>
								<a class="dropdown-item" href="{{path('recette_admin',{'page':1 })}}">Catalogue Admin</a>
							</li>
						{% else %}
							<a class="dropdown-item" href="{{path('recette',{'page':1 })}}">Catalogue</a>
						{% endif %}
						<li>

							{% if  app.user and "ROLE_ADMIN" in app.user.roles %}
								<a class="dropdown-item" href="{{path('new_recette_admin')}}">Ajouter Admin</a>

							{% else %}
								<a class="dropdown-item" href="{{path('new_recette')}}">Ajouter</a>
							{% endif %}
						</li>
						<li>
							<a class="dropdown-item" href="{{path('list')}}">Favoris</a>
						</li>
					</ul>
				</li>

				<li>
					<a class="nav-link " href="{{path('contact')}}">Contact</a>
				</li>
			</ul>
			<form class="d-flex" action="{{path('search',{'page':1 })}}">
				<input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" name="search_value">
				<button class="btn btn-outline-success" type="submit">Search</button>
			</form>
			<div class="col-md-4 text-center text-md-end align-self-center my-3 ">
					{% if app.user %}

					<a href="{{path('app_logout')}}" class="btn btn-outline-success">Deconexion</a>

					{% else %}

					<a href="{{path('app_register')}}" class="btn btn-outline-success">Register</a>


					<a href="{{path('app_login')}}" class="btn btn-outline-success">Login</a>


					{% endif %}

				</div>
		</div>
	</div>
</nav>
