{% extends 'base.html.twig' %}
{# Vue du formulaire pour permetre a l'utilisateur de recevoir un mail de changement de mot de passe #}
{% block title %}BecomeChef - Mot de passe oublie{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row w-75 m-auto justify-content-center">
        <div class="col-6 text-center py-5 ">
            <h1 class="titres coloredText text-center fs-1">Réinitialisation mot de passe</h1>
            <div class="">
            {# le formulaire #}
                {{ form_start(form_reset) }}
                {{form_label(form_reset.email)}}
                {{ form_widget(form_reset.email, {'attr' : {
                    'placeholder' : 'Votre adresse mail',
                    'class': 'formInput'
                }}) }}
                <button class="btn FormSub w-50 text-dark fs-5 m-auto mt-2" type="submit">Envoyer</button>
                {{ form_end(form_reset) }}

            </div>
</div>

    </div>
</div>

{% endblock %}
